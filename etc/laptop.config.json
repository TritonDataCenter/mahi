{
    "ufds": {
        "url": "ldap://127.0.0.1:1389",
        "_comment": "max connections must be 1, we do not want to use pooling",
        "maxConnections": 1,
        "bindDN": "cn=root",
        "bindCredentials": "secret"
    },
    "redis": {
        "host": "127.0.0.1",
        "port": 6379,
        "options" : {}
    },
    "pollInterval": 14000
}
