{
    "ufds": {
        "url": "{{UFDS_URL}}",
        "_comment": "max connections must > 1, we do want to use pooling",
        "maxConnections": 2,
        "bindDN": "{{UFDS_ROOT_DN}}",
        "bindCredentials": "{{UFDS_ROOT_PW}}",
        "interval": 10000
    },
    "redis": {
        "host": "localhost",
        "port": 6379,
        "options" : {}
    }
}
